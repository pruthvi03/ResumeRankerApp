<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.asus.myproject">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.VIBRATE"/>

    <application
        android:allowBackup="true"

        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity
            android:name=".MyProfileActivity"
            android:theme="@style/NoAct" />
        <activity
            android:name=".SearchActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".StartActivity"
            android:theme="@style/NoAct" />
        <activity
            android:name=".SignUpActivity"
            android:theme="@style/NoAct">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SignInActivity"
            android:theme="@style/NoAct" />
        <activity android:name=".VacancyInfoActivity" />
        <activity android:name=".RecActivity" />
        <activity android:name=".CandidateMoreInfo" />
        <activity android:name=".CompanyMoreInfo" />
        <activity
            android:name=".QuizActivity"
            android:theme="@style/NoAct" />
        <activity
            android:name=".GraphActivity"
            android:theme="@style/NoAct" />
        <activity android:name=".CandMarkActivity"></activity>
    </application>

</manifest>